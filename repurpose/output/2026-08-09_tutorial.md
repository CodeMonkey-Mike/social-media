# Lane 3 drafts — batch `tutorial` (2026-08-09)

Source: `livestream-repurpose/transcripts/tutorial LOW BPS VERTICAL/tutorial LOW BPS VERTICAL_plain.txt`
Stream: 76.6 min, 2026-08-08 — TUT on BNB pumping again (the 94x call), altseason waking up,
Clarity Act September date, Robinhood-chain meme rankings ($IF > Cooper > Tendies), doginme math,
Kaspa/Binance community-coin catch-22, ElizaOS lesson.

**Executed via the Wave 6 repurpose graph** — full copy + image prompts live in
`tutorial-lane3-plan.json` (the seam artifact); this file is the human-readable record.

## Fact-check (all verified 2026-08-09 before drafting)

- **TUT**: $0.155, +132% 24h, **+814% 7d**, mcap ~$130M, **new ATH $0.29 today (Aug 9)** — CoinGecko.
  94x + "550x seven days later" = Mike's community claims from the stream, used as his own claims.
- **doginme**: mcap ~$4.09M now (CoinGecko); ATH mcap ~$107M (Mike's chart read; CG-derived ≈ $110M).
  $400M scenario = 100x from here: math checks. On Coinbase ✓, first dog coin from Base ✓.
- **Clarity Act**: vote slipped past Aug recess (CoinDesk 08-06); Senate back Sept 14, first
  procedural vote ~Sept 15-16 → "a real Senate date in September" ✓.
- **Binance community-coin blog / Neiro listing (late 2024)** ✓ matches the record.
- **@tutorialtoken** verified via tutorialtoken.com → used in Follow line; **added to
  persona.json project_handles pending Mike's veto**.

## The 6 X tweets (4 long + 2 one-liners, all imaged)

1. **TUT vindication** (stat-stack + conviction) — img `3013e2fd` (plain amber coin + graduation cap;
   no TUT logo exists in reference/, identity carried by the education metaphor).
2. **Kaspa/Binance catch-22** (named-opposition, tribal) — img `72ee4bf0` (Kaspa backwards-K teal vs
   Binance-diamond bouncer). **Engagement note: correction-bait technique #3 ("What am I missing?")**
   — the 1-in-10 slot for this batch. → IG 4:5 companion (the Kaspa entry).
3. **Robinhood-chain meme rankings** ($IF/Cooper/Tendies, his exact conviction order) — img `cd7c52e0`
   (podium, plain coins + generic retriever, Robinhood lime-green spotlights per brand rule).
4. **doginme 100x math** (specific-number prediction) — img `5c31a153` (ref `DogInMe.png`).
5. One-liner: **"Nobody announces altseason..."** — img `d30a70e4`.
6. One-liner: **94x + 550x euphoria memory** — img `3b0059fe`.

## Skips + flags

- **X polls: 1 queued (Kaspa/Binance)** — passes the hard topic filter. The RH-meme poll is
  YT-only (memecoin topic, not Kaspa/TAO/Toncoin).
- **IG: exactly 1 entry** (`ig-2026-08-09-kaspa-binance-catch22`) — the only Kaspa-subject image
  this batch. Correct under the Kaspa-only hard rule.
- **Carousel versions:** post A = **V1** (news-flash energy, pinned to the one clean exemplar
  `828eee71-01-hook` per the contamination doctrine); post B = **V2** (editorial/framework,
  role-matched exemplars). **V4 skipped again: still no topical hook-photo reference set.**
- **NOT named in copy**: the "550x" token (Whisper garbled "NYX"/"my X on BNB" — unverifiable, so
  the claim rides unnamed); Mike's private AI community token (he explicitly hasn't made it
  public); Yolo/Swappy/Cash Cat numbers (chart reads, poll options only, no numbers).
- **Missing references:** CashCat PNG · $IF/$COOPER/Tendies X handles · V4 hook-photo set.
  (Cooper + TUT + Tendies references were supplied by Mike and saved 2026-08-09, below.)

## Image revision v2 (Mike, 2026-08-09) — real project identities

Mike supplied three X-profile references; all saved to `schedule-tweets/images/reference/` and
visually verified: **`TUT-tutorial.jpg`** (amber square, black italic T with a lightning slash),
**`cooper.jpg`** (black lab, lime bandana carrying the Robinhood mark), **`tendies.jpg`** (the X
banner: hooded Pepe holding a platter of chicken tenders). `what-if.jpg` was already on disk and
is exactly the $IF art (green figure from behind gazing at a spiral galaxy).

Two images were REGENERATED in place (same `image_id`, same slug, same filename, so the queue
entries never moved; the v1 renders were moved aside and passed back in as the base reference so
the scenes survived):

- **`3013e2fd` tut-94x-pumping-again** — refs `[v1 render, TUT-tutorial.jpg]`. Scene held identical;
  the coin's blank face now carries the real $TUT glyph. The glyph is the only marking in frame.
- **`cd7c52e0` rh-meme-podium** — refs `[v1 render, what-if.jpg, cooper.jpg, tendies.jpg]`, each
  addressed BY ORDINAL. 1st = the $IF coin with the galaxy artwork inset in its face · 2nd = Cooper
  as a genuine BLACK lab (v1 rendered a golden retriever) with his lime bandana · 3rd = Tendies as
  the hooded frog holding the tender platter, because a coin cannot carry "chicken tenders"
  (Mike's call: "maybe we need to change it a little bit differently"). The prompt explicitly
  excluded the banner's lettering, its suit figure, its candlestick chart and its rocket, and all
  four exclusions held.

Both v2 renders visual-QA PASS first try. Method note worth keeping: **a 4-reference generation
works when every reference is addressed by ordinal with its role AND an explicit do-not-copy list
for the parts of a busy reference you do not want** (the tendies banner is mostly furniture).
