---
name: higgsfield-generate
description: Generate images/videos via Higgsfield AI (GPT Image 2, Seedance 2.0 video, Nano Banana 2/Pro, Soul V2/Cinema, Marketing Studio, Virality Predictor). Use across all video-creation tracks. POINTER ONLY — the real skill is video-creation/skills/higgsfield-generate/SKILL.md in this repo.
---

# /higgsfield-generate — POINTER (not the skill)

**This file is a pointer, not the skill.** The canonical, repo-vendored Higgsfield Generate skill lives at:

- Repo-relative: `video-creation/skills/higgsfield-generate/SKILL.md` (+ its `references/` folder)
- Absolute: `C:\Users\mnede\Documents\Claude\social-media\video-creation\skills\higgsfield-generate\SKILL.md`

When `/higgsfield-generate` is invoked, **READ that `SKILL.md` in full and follow it**, loading
`references/*.md` on demand as it instructs.

Prerequisite tooling (the `higgsfield` CLI + login) is NOT in the repo — see
`video-creation/SKILLS-SETUP.md` to install it on a fresh machine.

Do NOT copy the real content into this pointer. The repo copy under `skills/` is the single source of
truth (there is no global duplicate — removed 2026-06-19); update the skill only THERE.
