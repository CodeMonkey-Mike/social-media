---
name: video-creation
description: Mike's canonical video-creation pipeline — topic finding (90s chunk-and-group), snap-to-silence + declick cutting, Whisper captions, and b-roll/Remotion production. POINTER ONLY — the real skill is video-creation/SKILL.md at the repo root.
---

# /video-creation — POINTER (not the skill)

**This file is a pointer, not the skill.** The canonical Video Creation skill lives at the
repo root:

- Repo-relative: `video-creation/SKILL.md`
- Absolute: `C:\Users\mnede\Documents\Claude\video-creation\SKILL.md`

When `/video-creation` is invoked, **READ that root `SKILL.md` in full and follow it.**

Do NOT edit techniques here, and do NOT copy the real content into this file — edit the root
`SKILL.md`. This 3-line pointer exists only so `/video-creation` works as a command while the
real, canonical doc stays plainly visible at the repo root (Mike's explicit preference). If you
are ever tempted to "sync" content into here, don't — that recreates the divergence that caused
lost work on 2026-05-25.
