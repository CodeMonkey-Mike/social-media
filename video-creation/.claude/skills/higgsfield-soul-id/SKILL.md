---
name: higgsfield-soul-id
description: Train a Soul Character (personalized face model) on Higgsfield for identity-faithful image/video generation; returns a reference_id used by higgsfield-generate via --soul-id. POINTER ONLY — the real skill is video-creation/skills/higgsfield-soul-id/SKILL.md in this repo.
---

# /higgsfield-soul-id — POINTER (not the skill)

**This file is a pointer, not the skill.** The canonical, repo-vendored Higgsfield Soul ID skill lives at:

- Repo-relative: `video-creation/skills/higgsfield-soul-id/SKILL.md` (+ its `references/` folder)
- Absolute: `C:\Users\mnede\Documents\Claude\social-media\video-creation\skills\higgsfield-soul-id\SKILL.md`

When `/higgsfield-soul-id` is invoked, **READ that `SKILL.md` in full and follow it**, loading
`references/*.md` on demand as it instructs.

Prerequisite tooling (the `higgsfield` CLI + login) is NOT in the repo — see
`video-creation/SKILLS-SETUP.md` to install it on a fresh machine.

Do NOT copy the real content into this pointer. The repo copy under `skills/` is the single source of
truth (there is no global duplicate — removed 2026-06-19); update the skill only THERE.
