---
name: publish-shorts
description: Publish a fully-produced, approved batch of shorts to the schedule-tweets pipeline — copy the rendered mp4s into schedule-tweets/shorts/<batch>/ and append fully-structured entries to data/shorts.json (open-loop hook titles, clean no-hashtag captions, CryptoRich.vip link only on yt/rumble/bitchute, no em dashes). Use when Mike says "publish the batch", "send the shorts over", "queue these shorts", or "/publish-shorts". POINTER ONLY — the real skill is video-creation/PUBLISH-SHORTS.md at the repo root.
---

# /publish-shorts — POINTER (not the skill)

**This file is a pointer, not the skill.** The canonical Publish-Shorts skill lives plainly at the
video-creation root:

- Repo-relative: `video-creation/PUBLISH-SHORTS.md`
- Absolute: `C:\Users\mnede\Documents\Claude\social-media\video-creation\PUBLISH-SHORTS.md`

When `/publish-shorts` is invoked (or Mike asks to publish/queue an approved batch of shorts),
**READ that root `PUBLISH-SHORTS.md` in full and follow it.**

Do NOT copy the real content into this file — edit the root `PUBLISH-SHORTS.md`. This pointer
exists only so the command works while the canonical doc stays visible at the project root
(Mike's explicit convention, mirroring the `/video-creation` pointer).
