# need lang-graph — PROJECT-LOG

**Video 2** of **[@aiEngineeringSimplified](https://www.youtube.com/@aiEngineeringSimplified)**.
Title (LOCKED 2026-07-06): **"When Do You Actually Need LangGraph? (A Real-World Walkthrough)"**
Topic source: `langgraph-video-outline.md` (Claude-mobile discussion outline, built for ~34 min).
Track: **ai-engineering** (same pure-VO format as video 1 `../python-ai-libraries/`; see its PROJECT-LOG
for the full pipeline reference).

---

## ▶ RESUME HERE

**Where we are (2026-07-06):** Mike decided the video is **10-12 min**, not 30. Full SCRIPT.md drafted as
the compressed cut of the outline (all 8 segments kept at explainer depth; nothing dropped except the Seg-7
approval-dashboard build detail, which is teased as a follow-up). Then two Mike decisions applied same day:
**(a) code cards** — API-mechanism chunks show real 3-5 line snippets, and **(b) narrated code** — the VO
walks the viewer through what code to write (reference-the-screen device), not just concepts over silent
snippets. Script is now **12 chapters / 44 chunks ≈ 11 min**; code chunks: 5, 6, 8-9, 13, 14-15, 25, 29,
32, 35.

| Stage | Status |
|---|---|
| Length decision (10-12 min, Mike) | ✅ 2026-07-06 |
| Script (`SCRIPT.md`, 12 chapters / 44 chunks, ≈11 min, narrated code cards) | ✅ Mike said "proceed to build" 2026-07-06 (= approved to produce; VO tone blessed at draft review) |
| Title | ✅ LOCKED: "When Do You Actually Need LangGraph? (A Real-World Walkthrough)" |
| Render endpoint (Mike, 2026-07-06) | ✅ DECISION: draft render (~300k) then PAUSE for Mike's review + VO-tone blessing; HQ render only after confirm |
| Accuracy pass (mandatory pre-VO gate) | ✅ DONE 2026-07-06 (agent, vs live LangGraph 1.0 docs). 23 claims: 19 ✅, 4 fixed. Fixes applied to SCRIPT + containers: **c25** SqliteSaver.from_conn_string is a context-mgr not a saver → switched to `sqlite3.connect` + `SqliteSaver(conn)` constructor (needs `langgraph-checkpoint-sqlite`); **c14** Send list comes from a ROUTING FUNCTION via add_conditional_edges (not a node; also fixes c9 conflict), import = `from langgraph.types import Send`; **c24** softened "default checkpointer" (LangGraph has none). All other snippets verified current. |
| `tts-chunks.json` (pronunciation map + caps rule applied) | ✅ 44 chunks, spoken spellings (lang graph / sequel lite / L C E L), caps-on-short-words rule enforced (0 em dashes) |
| Voiceover (MIKE-CLONE via `skills/higgsfield-voice/_batch-generate.js`) | ✅ 44/44 generated + downloaded + ffprobe-valid. Stitched `audio/full-narration.mp3` = 702.5s (11.71 min). NOTE: voice guard needed the hfbot Chrome window WIDENED (was 1036px; guard wants selected voice at x>820) — set via `Browser.setWindowBounds` width 1680. MIKE-CLONE confirmed. |
| Audio-QA (agent, whisper vs sent text as chunks land) | ✅ 42/44 clean, 2 flagged (chunk-27 + chunk-29, word "Posting" onset garble → whisper heard "Running"/"Hosting"). RE-ROLLED both: chunk-29 fixed by CAPS emphasis (POSTING); chunk-27 needed a "But posting..." lead-in to fix the sentence-final onset. Both whisper-clean now; tts-chunks.json + SCRIPT updated; re-stitched + timeline regenerated (VO_END 700.37s). |
| Timeline (`remotion/src/needLangGraphTimeline.ts`) | ✅ regenerated with REAL durations (0 estimated), VO_END 702.48s, comp NLG_DURATION = 21122 frames |
| Containers (`deck/containers.html`, one full-frame per chunk) | ✅ all 44 rendered @2x (3840x2160). Container-QA agent: 43 OK + 1 MAJOR (c16 loop label clipped) → FIXED + re-rendered. Reused python-ai-libraries CSS + graph-diagram helpers. |
| Container-render script | ✅ `scripts/render-containers.js` (Playwright, `#cNN`→`render-assets/container-NN.png`) |
| Remotion comp `NeedLangGraph.tsx` + registered in Root | ✅ built + registered (id `NeedLangGraph`, 1920x1080). Timeline module `remotion/src/needLangGraphTimeline.ts` generated by `scripts/build-timeline.js` (currently ESTIMATED — regen after audio downloads). |
| 300k draft render | ✅ DONE → `need-langgraph-draft-300k.mp4` (in this project folder; moved out of the shared `remotion/out/`) (11.7 min, 1920x1080, 21059 frames, VO-only, 39.9MB). ⚠️ RENDER GOTCHA: a straight-through Remotion render dies at ~frame 3800 every time (headless-Chrome memory accumulation; NOT an external kill — reproduced twice). FIX: render VIDEO-ONLY in 7 foreground segments of ~3000 frames (`out/seg/s01..s07.mp4`, `--muted --frames=A-B`; fresh Chrome per segment resets memory), concat `-c copy`, then mux `audio/full-narration.mp3` (its 0.35s-gap timing == the timeline starts, so A/V is frame-exact). Draft is VO-only (music bed deferred to final). USE THIS SEGMENTED METHOD FOR THE FINAL HQ RENDER TOO. |
| Inline draft QA | ✅ blackdetect: none >0.5s · audio mean −16.4 dB / peak −1.1 dB (no clip) · 3 sample frames (chunks 8/25/outro) correct, code legible |
| Video-QA (agent, /watch transcript-sync + posting-word recheck) | ✅ PASS — 0 defects. Full transcript matches all 44 chunks in order; "posting" confirmed clean at 7:22 + 7:54; 10+ sync spot-checks aligned (<1s); no black/frozen/dup frames; audio continuous. Note: fps-sampled overlap frames are the intentional chapter crossfades. |
| **DRAFT READY FOR MIKE** | ✅ `need-langgraph-draft-300k.mp4` (in this project folder) — awaiting VO-tone blessing before final HQ render |
| Full production render (2 Mbps, WITH music bed) | ✅ DONE 2026-07-07 → `need-langgraph-final-2mbps.mp4` (project folder). Same 7-segment fresh-Chrome method at `--video-bitrate=2M`; concat `-c copy`; audio = VO + corporate bed mixed in ffmpeg replicating the comp envelope (music vol 0.056 ≈20 dB under VO, fade-in 1s, fade-out frames 20999-21053). 701.87s, video 2.00 Mbps, AAC 178k mono, 192 MB. Audio-QA: overall −16.4 dB mean / −1.0 dB peak (no clip, = VO-only draft so bed is subtle); music-only tail −43.7 dB. Driver: `remotion/out/seg/_render_final.sh`. |
| Thumbnail (Higgsfield Nano Banana Pro, style-matched to video-1) | ✅ DONE 2026-07-07 → `need-langgraph-thumbnail.png` (2752x1536) + `need-langgraph-thumbnail-1280x720.jpg` (upload-ready, 111 KB). Style = video-1 "EVERYTHING YOU NEED TO KNOW" (dark teal bg, two-tone ALL-CAPS headline top-left, chaos→green-arrow→order composition). Headline: "DO YOU ACTUALLY NEED / LANGGRAPH?" (LANGGRAPH emerald). Visual: tangled node-spaghetti → emerald arrow → clean StateGraph w/ feedback loop. Model `nano_banana_2`, 16:9, 2k, video-1 YT thumb (`wwQPmOMdbRA`) passed as `--image` style ref; text rendered crisp. NOTE: reused gpt_image_2-avoidance rule — used Nano Banana Pro, not gpt_image_2 via CLI. |
| Vertical (9:16) repurpose | ✅ DONE 2026-07-07 → `need-langgraph-VERTICAL-v1.mp4` (1080x1920, 2.0 Mbps, 701.87s, 192 MB). Per `longform-edited/skills/vertical-repurpose.md`. 44 containers reflowed to portrait in `deck/containers-vertical.html` (single-col compares, all 15 horizontal graph diagrams redrawn top-to-bottom, phone-tuned type) → `render-assets/vertical/container-NN.png` via `scripts/render-containers-vertical.js`. Comp `remotion/src/NeedLangGraphVertical.tsx` (1080x1920, reuses `needLangGraphTimeline.ts` + VO + music + zoom/dissolve verbatim; registered in Root). Rendered with the same 7-segment fresh-Chrome method (`remotion/out/seg-v/_render_vertical.sh`); audio reused verbatim by copying the stream out of the 16:9 final. QA: blackdetect none >0.4s, 6 concat seams seamless, audio parity EXACT (−16.4 dB mean / −1.0 dB peak == 16:9), portrait framing spot-checks across all content types clean. Separate deliverable — stage into vertical queues only on Mike's go. |
| Publish to @aiEngineeringSimplified | ⬜ (pending Mike's go) |

**Next action (2026-07-07):** FINAL render is DONE — `need-langgraph-final-2mbps.mp4` (2.0 Mbps, WITH the
corporate music bed, 11.7 min). Only step left is **publish to @aiEngineeringSimplified** on Mike's go.
If he flags a chunk's tone: re-roll that chunk (higgsfield-voice), re-stitch, regen timeline, re-render
that segment range at 2M, re-concat, re-mux (driver `remotion/out/seg/_render_final.sh`).

---

## The video at a glance
- **Format:** pure MIKE-CLONE voiceover over full-frame containers/diagrams. No face (same as video 1).
- **Register:** persona gear 2, peer-EXPERT authority (hard rule, never co-beginner).
- **Runtime target:** 10-12 min (44 chunks at video-1 pacing ≈ 11 min).
- **Narrated code (Mike, 2026-07-06):** teaching an API mechanism = show a real snippet AND talk through
  it in the VO ("look at the return line right here"). Concept-only chunks stay diagram-only.
- **Running example:** Mike's real content pipeline (livestream → 3 lanes → 7-platform publish) mapped
  onto a LangGraph StateGraph. Crypto specifics stay off-screen; it's just "a content pipeline".
- **Recurring visual:** ONE animated pipeline/graph diagram, reused and re-dressed across ch1, 4, 6, 9,
  10 (the outline's b-roll note). Build it once, restyle per chapter.

## Compression decisions (34 min outline → 10-12 min)
1. **All 8 segments kept**, each cut to explainer depth (2-4 chunks instead of 4 min).
2. Seg 7's local approval-dashboard build: **teased only** (its own future video).
3. "Exactly once vs at least once": one line inside the retry-policy chunk (27), not a section.
4. Agent-architecture scenarios (Seg 3): one concrete line per shape.
5. Follow-up teasers in the CTA: observability/tracing, scheduling, migration walkthrough (matches the
   outline's exclusion note).

## Reused infrastructure from video 1
- Two-file convention: readable `SCRIPT.md` + committed `tts-chunks.json` (exact Higgsfield text).
- `../python-ai-libraries/scripts/stitch-narration.js` (chunk stitcher, path-agnostic).
- `skills/higgsfield-voice/_batch-generate.js` + pronunciation map + caps rule (in that SKILL.md).
- Container rule: standalone full-frame containers (`longform-edited/skills/broll-and-containers.md` +
  `container-reference/`), built in `deck/containers.html`, never cropped from a scroll deck.
- Draft renders at low bitrate first (200-300k) for Mike's review; HQ render only after approval.
- Music: corporate bed ~19 dB under VO (video 1 used `assets/music/corporate/dmitriysimf_*` at −25 dB gain).

## Chronology
- **2026-07-05 (mobile):** Mike + Claude discussion → `langgraph-video-outline.md` (34-min, 8 segments).
- **2026-07-06 — scripting.** Mike set length to 10-12 min. Drafted the compressed SCRIPT.md (all 8 outline
  segments kept at explainer depth → 12 chapters). Then two Mike calls same day: (a) **code cards** on every
  API-mechanism chunk, and (b) **narrated code** — the VO walks through what to write, not concepts over silent
  snippets. This grew the script to **44 chunks (~11 min)**; code chunks 5,6,8-9,13,14-15,25,29,32,35.
- **2026-07-06 — "proceed to build" (this session).** Mike: build the video end-to-end — deck+containers,
  capture MIKE-CLONE audio, use an agent team (audio-QA + container-QA + video-QA running while I create),
  render, report. Two decisions locked via question: **draft render then PAUSE** for his review + VO-tone
  blessing (not straight to HQ); **title locked**.
- **Accuracy pass (agent, vs live LangGraph 1.0 docs).** 23 claims, 19 ✅, 4 fixed — the load-bearing one:
  `SqliteSaver.from_conn_string` is a context manager, not a saver (the snippet wouldn't run) → constructor
  form. Plus the Send-from-a-routing-function reframe (also fixed a c9 conflict) and the "default checkpointer"
  softening. Fixes applied to BOTH SCRIPT.md and the code-card containers.
- **Containers.** Built `deck/containers.html` (44 standalone full-frame containers) reusing video-1's CSS +
  new graph-diagram helpers (SVG fan-out/fan-in, supervisor/swarm/hierarchy, guard steps, 2x2). Wrote
  `scripts/render-containers.js` (Playwright, @2x). Container-QA agent: 43 OK, 1 MAJOR (c16 DAG loop label
  clipped by the schedule node) → moved the loop arc to its own SVG, re-rendered, verified.
- **Audio.** Built `tts-chunks.json` (spoken spellings + caps-on-short-words rule; 0 em dashes). Higgsfield was
  logged in with MIKE-CLONE, but the batch driver's voice guard failed on a narrow (1036px) window — WIDENED
  the hfbot Chrome to 1680 via CDP `Browser.setWindowBounds`, guard passed. Generated 44/44 (0 fails), all
  downloaded + ffprobe-valid. Audio-QA agent whisper-checked as they landed: 42/44 clean, flagged the word
  **"Posting"** garbling in chunks 27 + 29 (load-bearing — the idempotency point). Re-rolled both: c29 fixed
  with CAPS emphasis; c27 needed a "But posting..." lead-in to fix the soft sentence-final onset. Both clean.
- **Assembly.** Re-stitched `audio/full-narration.mp3` and regenerated `needLangGraphTimeline.ts` off REAL
  durations (VO_END 700.37s). Built + registered comp `NeedLangGraph`; preflight still render validated the
  pipeline.
- **Draft render saga.** A straight-through Remotion render **died at ~frame 3800 every time** (headless-Chrome
  memory accumulation; reproduced twice — NOT an external kill, and a `TaskStop` on the wrapper left the node
  render orphaned, which then collided with the retry). ROOT-CAUSE FIX: render **video-only in 7 foreground
  segments** of ~3000 frames (`--muted --frames=A-B`; fresh Chrome per segment resets memory) → concat
  `-c copy` → mux `full-narration.mp3` (its 0.35s gaps == the timeline starts, so A/V is frame-exact). Draft
  is VO-only; music bed deferred to the final. **This method is the one to reuse for the HQ render.**
- **QA + pause.** Inline: no black >0.5s, audio −16.4 dB mean / −1.1 dB peak, sampled frames correct. Video-QA
  agent: **PASS, 0 defects** — transcript matches all 44 chunks in order, "posting" confirmed clean at 7:22 +
  7:54, 10+ sync spot-checks <1s, no black/frozen/dup frames. **PAUSED for Mike's VO-tone blessing** →
  `need-langgraph-draft-300k.mp4` (in this project folder; 11.7 min).
- Agent team used (per Mike's ask): accuracy-pass, container-QA, audio-QA (ran while I built the comp),
  video-QA — 4 background agents total, each returning a structured report folded into this log.
