Lane graph is a different animal.
You define nodes, you connect them with edges, and one shared state object flows through
the whole thing, and the code reads exactly like the picture.
Add node, add edge, compile.
The graph you draw on the whiteboard is the program.
