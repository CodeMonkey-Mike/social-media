Take a real rule like, never post to two platforms in parallel, don't ask the model nicely, build
one posting node that everything funnels through so the topology itself forbids parallel posts,
and never auto-retry a post, as that retry policy from a minute ago set to zero,
not a sentence in a prompt,
