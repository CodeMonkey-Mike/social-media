Lang chain is a box of components.
Model wrappers, prompt templates, output parsers, tools, retrievers, plus a piping
syntax called LCL that chains them together.
Look at the shape of it right here, prompt, pipe, model, pipe, parser.
A question goes in one end, clean data comes out the other.
